# Do not edit!
# Auto generated by sfSqlDesignerPlugin

# This is a table to store books
books:
  columns:
    id: { type: integer, primary: true, notnull: true, autoincrement: true }
    title: { type: string(128), notnull: true }
    # This is a short description of what the book is about
    excerpt: { type: string(255), notnull: true }
    published: { type: date, notnull: true }
    Authors_id: { type: integer, notnull: true }
  relations:
    authors:
      local: Authors_id
      foreign: id

# This table stores the books authors
authors:
  columns:
    id: { type: integer, primary: true, notnull: true, autoincrement: true }
    name: { type: string(128), notnull: true }

